#!/usr/bin/env bash
#PBS -N dva_compute_benchmark
#PBS -l select=1:ncpus=2:mem=32gb
#PBS -l walltime=72:00:00
#PBS -o logs/cluster/ems/compute_benchmark.out
#PBS -e logs/cluster/ems/compute_benchmark.err

set -euo pipefail
cd "${PBS_O_WORKDIR:-$(pwd)}"
bash scripts/cluster/ems/compute_benchmark.sh
